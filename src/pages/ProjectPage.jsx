import React from 'react';
import ProjectList from '../containers/ProjectList';

const ProjectPage = () => {
    return (
        <div>
            <ProjectList />
        </div>
    );
};

export default ProjectPage;